const categoryController = require('../categoryController')

describe('categoryController', () => {
	describe('index', () => {
		it('should return empty array when database was empty with status 200.', () => {})
		it('should return all category in array with status 200', () => {})
	})
	describe('create', () => {})
	describe('read', () => {})
	describe('update', () => {})
	describe('delete', () => {})
})
